apiVersion: apps/v1
kind: Deployment
metadata:
  name: library-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: library-web
  template:
    metadata:
      labels:
        app: library-web
    spec:
      containers:
        - name: library-web
          image: hamzashaukat078/library-web:latest
          ports:
            - containerPort: 3000

